import { Repository } from 'typeorm';
import { Consultation } from '../schedule/entities/consultation.entity';
import { User } from '../users/entities/user.entity';
import { ConsultationNotificationPreferencesService } from './consultation-notification-preferences.service';
import { Notification } from './notification.entity';
import { NotificationsGateway } from './notifications.gateway';
import { PushNotificationsService } from './push-notifications.service';
type ConsultationNotificationAction = 'created' | 'updated' | 'deleted';
export type ConsultationNotificationSnapshot = {
    id: number;
    departmentId: number;
    teacherId: number;
    teacherName: string;
    subject: string;
    consultationType: string;
    dayOfWeek: number;
    startTime: string;
    endTime: string;
    weekStart: string;
    room: string | null;
    comment: string | null;
};
export declare class ConsultationNotificationsService {
    private readonly notificationsRepository;
    private readonly usersRepository;
    private readonly notificationsGateway;
    private readonly pushNotificationsService;
    private readonly consultationNotificationPreferencesService;
    private readonly logger;
    constructor(notificationsRepository: Repository<Notification>, usersRepository: Repository<User>, notificationsGateway: NotificationsGateway, pushNotificationsService: PushNotificationsService, consultationNotificationPreferencesService: ConsultationNotificationPreferencesService);
    createConsultationSnapshot(consultation: Consultation): ConsultationNotificationSnapshot;
    notifyConsultationChanged(action: ConsultationNotificationAction, consultation: Consultation, previousItem?: ConsultationNotificationSnapshot): Promise<void>;
    private resolveConsultationRecipients;
    private hasConsultationChanges;
    private getConsultationTitle;
    private buildConsultationMessage;
    private buildUpdatedConsultationMessage;
    private getConsultationChanges;
    private isConsultationPlacementChanged;
    private formatConsultationTransferMessage;
    private formatConsultationSummary;
    private formatConsultationWithSubject;
    private formatConsultationDateTime;
    private formatConsultationDate;
    private formatConsultationShortDate;
    private getMessageAudience;
    private formatTeacherForNotification;
    private capitalize;
    private addChange;
    private formatTime;
}
export {};
