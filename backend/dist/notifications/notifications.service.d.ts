import { Repository } from 'typeorm';
import { ScheduleItem } from '../schedule/entities/schedule-item.entity';
import { User } from '../users/entities/user.entity';
import { Notification } from './notification.entity';
import { NotificationsGateway } from './notifications.gateway';
import { PushNotificationsService } from './push-notifications.service';
type ScheduleNotificationAction = 'created' | 'updated' | 'disabled' | 'deleted';
type ScheduleItemNotificationSnapshot = {
    id: number;
    groupId: number | null;
    groupName: string | null;
    subgroupId: number | null;
    subgroupNumber: number | null;
    teacherId: number | null;
    teacherName: string;
    subject: string;
    lessonType: string;
    room: string;
    weekStart: string;
    dayOfWeek: number;
    startTime: string;
    endTime: string;
    comment: string | null;
};
export declare class NotificationsService {
    private readonly notificationsRepository;
    private readonly usersRepository;
    private readonly notificationsGateway;
    private readonly pushNotificationsService;
    private readonly logger;
    constructor(notificationsRepository: Repository<Notification>, usersRepository: Repository<User>, notificationsGateway: NotificationsGateway, pushNotificationsService: PushNotificationsService);
    listForUser(userId: number): Promise<Notification[]>;
    markAsRead(userId: number, id: number): Promise<void>;
    markAllAsRead(userId: number): Promise<void>;
    notifyPreholidayDayCreated(date: string): Promise<void>;
    notifyScheduleItemChanged(action: ScheduleNotificationAction, item: ScheduleItem, previousItem?: ScheduleItemNotificationSnapshot): Promise<void>;
    createScheduleItemSnapshot(item: ScheduleItem): ScheduleItemNotificationSnapshot;
    private resolveRecipients;
    private addStudentRecipients;
    private findStudentsByGroupName;
    private normalizeGroupName;
    private getScheduleTitle;
    private buildPreholidayMessage;
    private buildScheduleMessage;
    private buildCreatedScheduleMessage;
    private getScheduleChanges;
    private buildUpdatedScheduleMessage;
    private isLessonPlacementChanged;
    private formatTransferMessage;
    private formatRoomChangeMessage;
    private formatLessonForRoomChange;
    private addChange;
    private formatLessonSummary;
    private formatTeacherLessonSummary;
    private formatLessonDateTime;
    private formatLessonDate;
    private formatShortLessonDate;
    private formatPair;
    private formatLessonType;
    private formatLessonWithSubject;
    private formatTeacherForNotification;
    private formatGroup;
    private getMessageAudience;
    private capitalize;
    private formatSubgroup;
    private formatSubgroupPart;
    private formatTime;
    private formatIsoDate;
    private isToday;
}
export {};
