# Production or staging API URL (required when frontend and backend are on different origins)
# VITE_API_URL=https://api.example.com

# Local dev: route API through Vite proxy instead of :3000
# VITE_USE_PROXY=true
